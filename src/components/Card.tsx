import * as React from "react";
import {observer} from 'mobx-react'

@observer
export class Card extends React.Component<{}, {}> {

    render() {
        return (
            <div className="card">
            
            </div>
        )
    }
}